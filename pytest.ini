[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Unit tests — no external services required (default)
    integration: Integration tests — requires a live MongoDB instance
    e2e: End-to-end tests — requires the full stack running
filterwarnings =
    ignore::DeprecationWarning
